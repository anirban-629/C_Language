#include<stdio.h>
int main(){
  float b,h;
  printf("Enter the Base: ");
  scanf("%f",&b);
  printf("Enter the Height: ");
  scanf("%f",&h);
  printf("Area of the triangle is: %.2f",0.5*b*h);
  return 0;
}
