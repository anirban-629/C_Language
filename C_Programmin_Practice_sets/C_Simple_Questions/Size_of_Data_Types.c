#include<stdio.h>
int main(){
  printf("Size of int as per my laptop's Architecture is    : %d\n",sizeof(int));
  printf("Size of float as per my laptop's Architecture is  : %d\n",sizeof(float));
  printf("Size of char as per my laptop's Architecture is   : %d\n",sizeof(char));
  printf("Size of double as per my laptop's Architecture is : %d\n",sizeof(double));
  return 0;
}
